defmodule Postern do
  @moduledoc """
  Postern is a Language Server Protocol server for PostgreSQL configuration
  files (`postgresql.conf`, `pg_hba.conf`, `pg_ident.conf`).

  See `Postern.Server` for the LSP implementation, `Postern.FileKind` for
  file detection, and `Postern.DocumentStore` for the in-memory document
  store.
  """
end
